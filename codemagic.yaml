workflows:
  react-native-android:
    name: My Scientific Calculator Android App
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      java: 17
      node: 20.11.1
      vars:
        CI: "1"
        EXPO_NO_TELEMETRY: "1"
    scripts:
      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
      - name: Set App Config and Prebuild
        script: |
          node -e '
          const fs = require("fs");
          if (fs.existsSync("app.json")) {
            let cfg = JSON.parse(fs.readFileSync("app.json", "utf8"));
            if (cfg.expo) {
              cfg.expo.name = "My Scientific Calculator";
              delete cfg.expo.icon;
              delete cfg.expo.splash;
              if (cfg.expo.android) {
                delete cfg.expo.android.adaptiveIcon;
                cfg.expo.android.package = "com.mohil.scientificcalculator";
              } else {
                cfg.expo.android = { package: "com.mohil.scientificcalculator" };
              }
            }
            fs.writeFileSync("app.json", JSON.stringify(cfg, null, 2));
          }
          '
          npx expo prebuild --platform android --clean
      - name: Build Standalone Offline Android APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon
    artifacts:
      - android/app/build/outputs/apk/**/*.apk
